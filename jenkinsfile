pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'pwd'
            }
        }
        stage('odc is the best') {
            steps {
                sh 'mkdir new_folder && ls -al'
            }
        }
    }
}
